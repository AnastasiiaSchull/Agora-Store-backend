name: 🧪 Test Self-Hosted Runner

on:
  push:
    branches:
      - main  

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: ✅ Проверка запуска
        run: echo "Runner работает!"

      - name: Кто я
        run: |
          echo "Пользователь: $(whoami)"
          echo "Дата: $(date)"
          echo "Путь: $(pwd)"
